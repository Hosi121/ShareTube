# Local development environment for ShareTube backend

# API server port
PORT=8081

# MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=example
DB_NAME=video_sns

# JWT secret for signing access tokens (change in production)
JWT_SECRET=change_me_dev_secret

# Optional: upload directory (default ./uploads)
# UPLOAD_DIR=./uploads
